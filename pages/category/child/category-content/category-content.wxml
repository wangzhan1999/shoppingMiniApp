<scroll-view scroll-y class="categorycontent">
  <view class="subcategory">
    <block wx:for="{{subCategories}}" wx:key="index">
      <view class="item">
        <image src="{{item.image}}" mode="widthFix"/>
        <view>{{item.title}}</view>
      </view>
    </block>
  </view>
</scroll-view> 